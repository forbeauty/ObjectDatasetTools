拍摄：
1. 控制距离，拍摄一组物体图
2. 拍摄同一物体的另一组图，重建后拼接
2. 拍摄一组混合多个物体图（不需要三维重建，需要目标识别）

生成LINEMOD数据集步骤：
1. 录制对齐的RGB图和深度图，计算内参和转换矩阵，重建3D物体
2. 多物体情况下针对单一物体，目标识别，计算边界框，实例分割
3. 估计物体位姿

packages requirement:


cd .. && cd ..
cd LINEMOD && rm -rf LINEMOD/